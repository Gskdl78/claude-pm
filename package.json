{
  "name": "claude-pm",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --root plugin",
    "test:watch": "vitest --root plugin"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
